Firmware release v1.0.0

This folder contains the firmware source files for release v1.0.0.

Files:
- esp_telemetry.ino: ESP8266 telemetry example (copy placeholder; see NODEMCU/firmware_examples for full working file).

To build the firmware:
- Use Arduino IDE or PlatformIO with ESP8266 board support.
- Open the `NODEMCU/firmware_examples/esp_telemetry.ino` from the root repo and flash to your NodeMCU.
